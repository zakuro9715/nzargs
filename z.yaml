tasks:
  setup:
    run:
      - go get github.com/eaburns/Watch
  watch:
    run:
      - Watch -t z test bench
  test:
    run:
      - go test -v -race .
    tasks:
      bench:
        run:
          - go test -v -bench .
